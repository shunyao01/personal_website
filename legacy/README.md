# Legacy static HTML

This folder is **not** how the site runs anymore.

The old `index.html` had its CSS `<link>` tags commented out, so opening it with Live Server shows an unstyled page.

Use the Next.js app instead:

```bash
npm run dev
```

Then open http://localhost:3000
